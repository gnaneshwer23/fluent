import { LucideIcon, Hospital, GraduationCap, TreePine, MapPin } from 'lucide-react';

export const IconMap: Record<string, LucideIcon> = {
  hospital: Hospital,
  university: GraduationCap,
  park: TreePine,
  default: MapPin
};
